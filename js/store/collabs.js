/* ===== colaboradores por foro: SOLO usuarios registrados reales =====
   Ya no hay datos semilla: la lista se llena con los usuarios que registran
   su cuenta y eligen un foro principal (ver db.js isCollab). */
export function getCollabs(boardId) {
  return [];
}